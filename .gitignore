node_modules/
*.log
.DS_Store

# Supabase CLI scratch dir (version cache etc.) — not project state.
supabase/.temp/

# Edge Function secrets (VAPID private key, webhook secret) — never commit these.
.env
.env.*
.env.example
supabase/.env
supabase/.env.*
supabase/.env.example
supabase/notifications-setup.sql